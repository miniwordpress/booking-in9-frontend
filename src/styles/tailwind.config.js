const { default: theme } = require("@material-tailwind/react/theme");

module.exports = {
    theme: {
        extend: {
            fontFamily: {
                prompt: ['Prompt', 'sans-serif'],
            },
        },
    },
}